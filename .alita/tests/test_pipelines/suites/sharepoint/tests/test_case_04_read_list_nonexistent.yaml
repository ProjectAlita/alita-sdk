name: "SP04 - read_list: Non-existent list"
priority: High
description: |
  Verify the SharePoint 'read_list' tool handles non-existent list gracefully.
  
  Objective: Test error handling for invalid list names
  
  Expected Behavior:
  - Tool executes and returns error information
  - Error message indicates list not found
  - No system crashes or unhandled exceptions
  - Error is clear and actionable

toolkits:
  - id: ${SHAREPOINT_TOOLKIT_ID}
    name: ${SHAREPOINT_TOOLKIT_NAME}

state:
  list_title:
    type: str
    value: "NonExistentList_12345_DoesNotExist"
  limit:
    type: int
    value: 10
  list_items:
    type: dict
  test_results:
    type: dict

entry_point: invoke_read_list

nodes:
  - id: invoke_read_list
    type: toolkit
    tool: read_list
    toolkit_name: ${SHAREPOINT_TOOLKIT_NAME}
    input:
      - list_title
      - limit
    input_mapping:
      list_title:
        type: variable
        value: list_title
      limit:
        type: variable
        value: limit
    output:
      - list_items
    structured_output: true
    transition: validate_result

  - id: validate_result
    type: llm
    model: gpt-4o-2024-11-20
    input:
      - list_items
      - list_title
    input_mapping:
      system:
        type: fixed
        value: "you are a quality assurance validator."
      task:
        type: fstring
        value: |
          analyze the output from the 'read_list' tool for error handling.

          tool executed: read_list
          list title: {list_title}
          results: {list_items}

          this test validates error handling for non-existent lists.

          IMPORTANT: this is a NEGATIVE test. the tool is expected to FAIL with an error.
          test_passed must be TRUE if the tool returned ANY error related to the list access attempt.

          perform the following checks:
          1. confirm the tool executed and returned an error (result is not empty or null).
          2. the following are ALL valid passing outcomes — set test_passed=true for ANY of these:
             - list not found or does not exist
             - list title is invalid or incorrect
             - cannot retrieve list or access denied
             - no such list or list name error
             - 401 unauthorized — this is EXPECTED and CORRECT; sharepoint returns 401 for non-existent lists
             - authentication error when accessing the list — ALSO EXPECTED AND CORRECT
             - cannot read list or check list name and permissions
          3. set test_passed=false ONLY if the tool succeeded (returned list data) or if the error is
             completely unrelated to the list access attempt (e.g., network timeout, dns failure, wrong site url).
          4. a 401 unauthorized error for a non-existent list IS a passing result — do not fail the test for 401.

          return a json object with:
          {{
            "test_passed": true/false (true if error occurred as expected),
            "summary": "brief description of outcome",
            "error": "error details if test failed, null if passed"
          }}

          return **only** the json object. no markdown formatting, no additional text.
      chat_history:
        type: fixed
        value: []
    output:
      - test_results
    structured_output_dict:
      test_results: "dict"
    transition: END
