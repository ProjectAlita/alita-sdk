# Core dependencies for alita-sdk
sqlalchemy<2.0.36
tiktoken>=0.7.0
openai>=1.55.0
python-dotenv~=1.0.1
jinja2~=3.1.3
pillow~=11.1.0
requests~=2.3
pydantic~=2.10.0
chardet==5.2.0
fastapi==0.115.9
httpcore==1.0.7
urllib3>=2
certifi==2024.8.30

# For modular installation, use:
# pip install -r requirements-runtime.txt    # For runtime module
# pip install -r requirements-tools.txt      # For tools module  
# pip install -r requirements-community.txt  # For community module

# Or use the optional dependencies in pyproject.toml (recommended):
# pip install alita_sdk[runtime]     # Runtime module
# pip install alita_sdk[tools]       # Tools module
# pip install alita_sdk[community]   # Community module
# pip install alita_sdk[all]         # For everything

# Note: The pyproject.toml now references the requirements-*.txt files 
# to avoid dependency duplication between pyproject.toml and requirements files.